<h2><%= @post.title %></h2>

<p><%= @post.body %></p>

<h3>Comments</h3>
<% @post.comments.each do |comment| %>
  <p><%= comment.body %></p>
  <%= link_to "Delete", comment_path(comment), method: 'delete' %>
<% end %>

<h5>Leave Comment</h5>

<%= render 'comment_form' %>
